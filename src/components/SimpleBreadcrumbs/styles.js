const styles = theme => ({
  root: {
    justifyContent: 'center',
    flexWrap: 'wrap',
    marginBottom: 20
  },
  paper: {
    padding: `${theme.spacing.unit}px ${theme.spacing.unit * 2}px`,
  },
});

export default styles;